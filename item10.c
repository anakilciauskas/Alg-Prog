#include <stdio.h>
#include <stdlib.h>

	int main(){
		int alunos = 64; //declara uma variavel do tipo inteiro e atribui a ela o valor 64
		printf("A quantidade de alunos e %d", alunos); //exibe na tela a frase com o valor da variavel
	
		return 0;
	}